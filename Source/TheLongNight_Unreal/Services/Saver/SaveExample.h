#pragma once

class SaveExample
{
public:
	// UMySaveGame* BuildSnapshot(UWorld* World)
	// {
	// 	auto* Save = Cast<UMySaveGame>(UGameplayStatics::CreateSaveGameObject(UMySaveGame::StaticClass()));
	// 	// Clock → Save->GameTime
	// 	// PlayerStats → Save->Player
	// 	// Inventory → Save->Inventory.Items
	// 	// World → Save->World
	// 	return Save;
	// }
};
